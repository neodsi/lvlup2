{% extends 'layout/public.html.twig' %}

{% block title %}{{ q ? 'Résultats « ' ~ q ~ ' »' : 'Trouver une école de danse en France' }} | LVL UP{% endblock %}

{% block head %}
{% set _seoTitle = q ? 'Écoles de danse « ' ~ q ~ ' » · LVL UP' : 'Trouver une école de danse en France · LVL UP' %}
{% set _seoDesc = 'Trouvez votre école de danse en France. Consultez les programmes, horaires et tarifs. ' ~ total ~ ' école' ~ (total > 1 ? 's' : '') ~ ' référencée' ~ (total > 1 ? 's' : '') ~ ' sur LVL UP.' %}
{% set _canonicalUrl = url('app_school_directory') %}
<meta name="description" content="{{ _seoDesc }}">
<link rel="canonical" href="{{ _canonicalUrl }}">
<meta name="robots" content="{{ q ? 'noindex, follow' : 'index, follow' }}">
<meta property="og:type" content="website">
<meta property="og:title" content="{{ _seoTitle }}">
<meta property="og:description" content="{{ _seoDesc }}">
<meta property="og:url" content="{{ _canonicalUrl }}">
<meta property="og:image" content="{{ absolute_url(asset('assets/screen-fr.jpg')) }}">
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="{{ _seoTitle }}">
<meta name="twitter:description" content="{{ _seoDesc }}">
<meta name="twitter:image" content="{{ absolute_url(asset('assets/screen-fr.jpg')) }}">
<script type="application/ld+json">
{
    "@context": "https://schema.org",
    "@type": "CollectionPage",
    "name": "Annuaire des écoles de danse en France",
    "description": "Trouvez une école de danse près de chez vous. Programmes, inscriptions et tarifs sur LVL UP.",
    "url": "{{ _canonicalUrl }}"
    {% if schools|length > 0 %}
    , "mainEntity": {
        "@type": "ItemList",
        "numberOfItems": {{ total }},
        "itemListElement": [
            {% for school in schools %}
            {
                "@type": "ListItem",
                "position": {{ loop.index + (pagination.currentPage - 1) * pagination.itemsPerPage }},
                "name": "{{ school.name|e('js') }}",
                "url": "{{ url('app_school_page', {citySlug: school.citySlug, schoolSlug: school.currentSlug}) }}"
            }{{ not loop.last ? ',' : '' }}
            {% endfor %}
        ]
    }
    {% endif %}
}
</script>
<meta name="subject" content="Annuaire écoles de danse France">
<meta name="classification" content="Sport, Danse, Annuaire">
<meta name="category" content="Danse">
{% endblock %}

{% block stylesheets %}
{{ parent() }}
<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css">
<style>
    .line-clamp-2 { display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }
    .card-hover:hover { box-shadow: 0 4px 16px rgba(0,0,0,.08); }
    #map .leaflet-popup-content { font-size: .8rem; }
</style>
{% endblock %}

{% block public_content %}
{# ── Layout split ── #}
<div class="pt-16 flex flex-col md:flex-row md:h-[calc(100vh-4rem)] md:overflow-hidden">

    {# ── Colonne gauche : recherche + liste ── #}
    <section class="md:w-[45%] md:overflow-y-auto flex flex-col" aria-label="Liste des écoles de danse">

        {# Barre de recherche #}
        <div class="sticky top-0 z-10 bg-white border-b border-gray-200 px-4 py-3 shadow-sm">
            <form method="GET" action="{{ path('app_school_directory') }}" role="search">
                <div class="flex gap-2">
                    <div class="relative flex-1">
                        <svg class="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-400 pointer-events-none" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-4.35-4.35M17 11A6 6 0 1 1 5 11a6 6 0 0 1 12 0z"/>
                        </svg>
                        <input type="search" name="q" value="{{ q }}"
                               placeholder="Ville ou nom d'école…"
                               aria-label="Rechercher une école de danse"
                               class="block w-full rounded-lg border border-gray-300 bg-gray-50 pl-9 pr-3 py-2.5 text-sm text-gray-900 focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-500/20 transition">
                    </div>
                    <button type="submit"
                            class="shrink-0 px-4 py-2.5 rounded-lg bg-indigo-600 text-white text-sm font-medium hover:bg-indigo-700 transition">
                        Rechercher
                    </button>
                </div>
            </form>
        </div>

        {# En-tête résultats #}
        <div class="px-4 pt-4 pb-2">
            <h1 class="text-2xl font-bold text-gray-900 mb-1">
                {% if q %}Résultats pour «&nbsp;{{ q }}&nbsp;»{% else %}Écoles de danse en France{% endif %}
            </h1>
            <p class="text-sm text-gray-500">
                {{ total }} école{{ total > 1 ? 's' : '' }} trouvée{{ total > 1 ? 's' : '' }}
            </p>
        </div>

        {# Liste des écoles #}
        <div class="px-4 pb-4 flex-1">
            {% if schools|length == 0 %}
            <div class="mt-12 text-center">
                <svg class="mx-auto w-12 h-12 text-gray-300 mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                </svg>
                <p class="text-gray-500 text-sm">Aucune école trouvée{% if q %} pour «&nbsp;{{ q }}&nbsp;»{% endif %}.</p>
                {% if q %}
                <a href="{{ path('app_school_directory') }}" class="mt-3 inline-block text-sm text-indigo-600 hover:underline">Voir toutes les écoles</a>
                {% endif %}
            </div>
            {% else %}
            <ul class="mt-1 space-y-2" role="list">
                {% for school in schools %}
                {% set _city = school.citySlug ? school.citySlug|replace({'-': ' '})|title : null %}
                <li>
                    <article class="card-hover bg-white rounded-xl border border-gray-100 transition"
                             data-school-slug="{{ school.currentSlug }}">
                        <a href="{{ path('app_school_page', {citySlug: school.citySlug, schoolSlug: school.currentSlug}) }}"
                           class="flex items-start gap-3 p-3">
                            <div class="shrink-0 w-16 h-16 rounded-lg overflow-hidden bg-gray-100 flex items-center justify-center">
                                {% if school.logoPath %}
                                <img src="{{ school.logoPath|imagine_filter('school_logo') }}"
                                     alt="{{ school.name }}" class="w-full h-full object-cover" loading="lazy">
                                {% elseif school.avatarPath %}
                                <img src="{{ school.avatarPath|imagine_filter('school_photo_main') }}"
                                     alt="{{ school.name }}" class="w-full h-full object-cover" loading="lazy">
                                {% else %}
                                <svg class="w-7 h-7 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-2 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/>
                                </svg>
                                {% endif %}
                            </div>
                            <div class="flex-1 min-w-0">
                                <h2 class="text-sm font-semibold text-gray-900 truncate group-hover:text-indigo-600">{{ school.name }}</h2>
                                {% if _city %}
                                <p class="text-xs text-gray-400 mt-0.5 flex items-center gap-1">
                                    <svg class="w-3 h-3 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
                                    {{ _city }}
                                </p>
                                {% endif %}
                                {% if school.description %}
                                <p class="text-xs text-gray-500 mt-1 line-clamp-2">{{ school.description|striptags|trim|slice(0, 130) }}</p>
                                {% endif %}
                            </div>
                            <svg class="shrink-0 w-4 h-4 text-gray-300 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
                        </a>
                    </article>
                </li>
                {% endfor %}
            </ul>

            <div class="mt-4">
                {% include 'components/_pagination.html.twig' with { pagination: pagination } %}
            </div>
            {% endif %}
        </div>

    </section>

    {# ── Carte OSM ── #}
    <div class="h-72 md:h-auto md:flex-1 relative isolate">
        <div id="map" class="w-full h-full"></div>
        {% if schools|length == 0 %}
        <div class="absolute inset-0 flex items-center justify-center bg-gray-100/80 pointer-events-none">
            <p class="text-sm text-gray-400">Aucun résultat à afficher</p>
        </div>
        {% endif %}
    </div>

</div>
{% endblock %}

{% block scripts %}
{{ parent() }}
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
<script>
window.SCHOOLS_MAP_DATA = {{ mapJson|raw }};

(function () {
    const map = L.map('map', { zoomControl: true });
    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
        attribution: '&copy; <a href="https://www.openstreetmap.org/copyright" target="_blank">OpenStreetMap</a>',
        maxZoom: 19
    }).addTo(map);

    const markerMap = {};
    const bounds   = [];

    (window.SCHOOLS_MAP_DATA || []).forEach(s => {
        if (!s.lat || !s.lng) return;
        const marker = L.marker([s.lat, s.lng]);
        marker.bindPopup(
            '<div style="min-width:140px;font-family:sans-serif">' +
            '<strong style="font-size:.85rem">' + s.name + '</strong>' +
            (s.addr ? '<br><span style="font-size:.75rem;color:#6b7280">' + s.addr + '</span>' : '') +
            '<br><a href="' + s.url + '" style="font-size:.75rem;color:#4f46e5;font-weight:600;margin-top:4px;display:inline-block">Voir l\'école →</a>' +
            '</div>'
        );
        marker.addTo(map);
        markerMap[s.slug] = marker;
        bounds.push([s.lat, s.lng]);
    });

    if (bounds.length > 0) {
        map.fitBounds(bounds, { padding: [40, 40], maxZoom: 13 });
    } else {
        map.setView([46.603354, 1.888334], 6);
    }

    document.querySelectorAll('[data-school-slug]').forEach(card => {
        card.addEventListener('mouseenter', () => {
            const slug = card.dataset.schoolSlug;
            if (markerMap[slug]) {
                markerMap[slug].openPopup();
                map.panTo(markerMap[slug].getLatLng(), { animate: true });
            }
        });
    });
}());
</script>
{% endblock %}
